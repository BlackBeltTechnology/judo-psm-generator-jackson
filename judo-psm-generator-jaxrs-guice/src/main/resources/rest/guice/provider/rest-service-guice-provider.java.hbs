{{> fragment.header.hbs }}

package {{ namedElementGuiceRestPackageName self }};

{{# if (hasDocumentation self) }}
    /**
    * {{ self.documentation.decorateWithAsterisks }}
    */
{{/ if  }}

public class {{ guiceRestServiceProviderClassName self }} implements com.google.inject.Provider<{{ namedElementApiRestFqName self }}> {
    @com.google.inject.Inject
    hu.blackbelt.judo.dispatcher.api.Dispatcher dispatcher;

    @Override
    public {{ namedElementApiRestFqName self }} get() {
        {{ namedElementImplRestFqName self }} impl = new {{ namedElementImplRestFqName self }}();
        impl.__bindDispatcher(dispatcher);
        return impl;
    }
}

